<form action="{{ path('bank')}}" method="post">

    <input type="hidden" name="moneyId" value="{{ moneyId }}"/>


    <button type="submit" class="btn btn-warning btn-lg  m-2">Отправить мне на счет в банке</button>


</form>
