<form action="{{ path('money-to-ball')}}" method="post">

    <input type="hidden" name="moneyId" value="{{ moneyId }}"/>

    <button type="submit">Перевести в Баллы по
        {{ course }}
    </button>
</form>
