{% extends 'base.html.twig' %}

{% block title %}Hello GameController!
{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px / 1.5 sans-serif;
        }
        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    {# <div class="example-wrapper">
                                <h1>Hello Gamer!</h1>
    
                                {# <button type="submit">To PLAY!</button> #}
    <a href="{{ path('prize') }}">To PLAY</a>


</div>
#}

<form
    action="{{ path('prize')}}" method="post">
    {# the argument of csrf_token() is an arbitrary string used to generate the token #}
    <input type="hidden" name="token" value="{{ csrf_token('game-token') }}"/>
    <input type="hidden" name="game" value="secret"/>

    <button type="submit">Delete item</button>
</form>{% endblock %}
